# this option is required for some DEs (e.g., Phosh) to be cleaned up fully so
# they aren't automatically restarted by a child process
rc_cgroup_cleanup="yes"

# UID of user to autologin
# AUTOLOGIN_UID=1000
